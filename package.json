{
  "name": "no-route-to-host",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A browser-based network troubleshooting game where the player is the on-call network engineer",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b && npm run build -w @nrth/frontend",
    "build:engine": "tsc -b",
    "typecheck": "tsc -b && tsc --noEmit -p packages/frontend",
    "lint": "tsc -b && tsc --noEmit -p packages/frontend",
    "dev": "npm run dev -w @nrth/frontend",
    "preview": "npm run preview -w @nrth/frontend",
    "test": "tsc -b && node packages/engine/dist/test/index.js",
    "validate": "tsc -b && node packages/validator/dist/index.js",
    "validate:hook": "node packages/hooks/dist/index.js",
    "watch:scenarios": "node packages/hooks/dist/index.js --watch",
    "mcp": "node packages/mcp-server/dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0"
  }
}
